/**
 * TODO: Create a Product class, and it contains attributes id, price and description. It also needs to have display() method which will return void
 * TODO: Create a Book class that needs to inherit from the Product class. The Book class will have author and title as attributes, and display method, which will return void
 * TODO: Create a Electronic class that needs to inherit from the Product class. The Electronic class will have brand and model as attributes, and display method, which will return void.
 */
